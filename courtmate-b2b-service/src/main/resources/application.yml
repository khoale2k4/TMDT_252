spring:
  application:
    name: courtmate-b2b-service
  datasource:
    url: ${DATABASE_URL:jdbc:postgresql://localhost:5432/postgres}
    username: ${DATABASE_USER:postgres}
    password: ${DATABASE_PASSWORD:denn231264}
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true
  security:
    jwt:
      secret-key: "404E635266556A586E3272357538782F413F4428472B4B6250645367566B5970"
      expiration: 3600000 # 1 ngày (ví dụ thêm)
      refresh-token:
        expiration: 604800000
payment:
  momo:
    secret-key: "your_secret_key_here"
misa:
  api:
    url: "https://api.misa.com.vn/invoice" # Thay bằng URL thật của MISA
    token: "your_misa_token_here"

ahamove:
  api:
    url: "https://api.ahamove.com" # Thêm sẵn cho luồng tiếp theo
server:
  port: 8081

jackson:
  serialization:
    write-dates-as-timestamps: false
  time-zone: Asia/Ho_Chi_Minh
  default-property-inclusion: non_null

logging:
  level:
    root: INFO
    vn.courtmate: DEBUG
    org.hibernate.SQL: DEBUG
    org.hibernate.type.descriptor.sql.BasicBinder: TRACE
